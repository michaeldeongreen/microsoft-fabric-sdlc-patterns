# Reusable workflow: Promote unsupported items via Fabric Deployment Pipelines.
#
# SKELETON — Not yet implemented.
#
# This is Job 2 of the sandwich pattern. Reserved for future item types that
# cannot be deployed via fabric-cicd or Git integration and must be promoted
# stage-to-stage using the Fabric Deployment Pipelines REST API.
#
# Note: Ontology and DataAgent are now deployed via fabric-cicd (Job 1) using
# logicalId references. This skeleton is kept for item types that may lack
# fabric-cicd support in the future.
#
# Called by: deploy-prod.yml (after deploy-supported succeeds)
#
# Future implementation:
#   - Authenticate via SPN and call the Deploy Stage Content API
#   - API: https://learn.microsoft.com/en-us/rest/api/fabric/core/deployment-pipelines/deploy-stage-content
#   - Selectively deploy only unsupported item types
#   - Note: The API does not support "related items" — each item must be listed explicitly

name: "Reusable: Deploy Unsupported Items"

on:
  workflow_call:
    inputs:
      environment:
        description: "Target environment (test, prod)"
        required: true
        type: string

permissions:
  contents: read

jobs:
  promote-unsupported:
    name: Promote unsupported items (${{ inputs.environment }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: ${{ inputs.environment }}
    steps:
      - name: Placeholder
        env:
          ENVIRONMENT: ${{ inputs.environment }}
        run: |
          echo "TODO: Promote unsupported items (e.g., Ontologies) via Deployment Pipeline API for $ENVIRONMENT"
          echo "This step will call the Fabric Deployment Pipelines REST API (Deploy Stage Content) once implemented."
